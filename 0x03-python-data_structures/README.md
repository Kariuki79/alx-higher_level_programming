| `0-print_list_integer.py` | A function that iterates through a list sequence and prints each integer item on a new line using `str.format()`. |
| `1-element_at.py` | A function that retrieves an element from a list at a specific index, mimicking C-style bounds checking. |
